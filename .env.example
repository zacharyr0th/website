########################################################
# Application Configuration
########################################################
# Domain Configuration
DOMAIN_NAME=example.com # Required: Your application's domain name
# Development Configuration
DEVELOPMENT_HOST=localhost # Optional: Development server host (defaults to 'localhost')
DEVELOPMENT_PORT=3000 # Optional: Development server port (defaults to '3000')
# Security
ENCRYPTION_KEY=your-encryption-key-here # Required: Used for data encryption
GOOGLE_SITE_VERIFICATION=your-verification-code # Optional: For Google Search Console verification
# Storage Configuration
STORAGE_ACCESS_KEY=your-access-key-here # Required: Cloud storage access key
STORAGE_SECRET_KEY=your-secret-key-here # Required: Cloud storage secret key
STORAGE_BUCKET_NAME=your-bucket-name # Required: Cloud storage bucket name
STORAGE_ENDPOINT=https://your-storage-endpoint # Required: Cloud storage endpoint URL
STORAGE_REGION=us-east-1 # Optional: Cloud storage region (defaults to us-east-1)
# Content Paths
ARTICLES_DIRECTORY=public/articles # Optional: Directory containing article markdown files (defaults to 'public/articles')
BIO_FILE_PATH=app/bio/fullbio.md # Optional: Path to bio markdown file (defaults to 'app/bio/fullbio.md')
########################################################
# Public Variables (Exposed to the Browser)
########################################################
NEXT_PUBLIC_SITE_URL=https://${DOMAIN_NAME} # Required: Public site URL
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX # Optional: Google Analytics ID
NEXT_PUBLIC_AUDIO_CDN_URL=https://your-cdn-url # Required: CDN URL for audio files
########################################################