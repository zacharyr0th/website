export interface ThemeColors {
  primary: string;
  secondary: string;
  accent: string;
  background: string;
  surface: string;
  muted: string;
  subtle: string;
  privvy: string;
  text: {
    primary: string;
    secondary: string;
  };
  border: string;
}
